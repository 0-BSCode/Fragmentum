/**
 * @deprecated This content script is deprecated as of the pivot to manual input.
 * Fragment generation is now handled via the popup where users paste
 * "Copy link to highlight" URLs from the browser's native feature.
 *
 * This file is preserved for reference and potential future use.
 * Last active: January 2026
 */

// // Fragmentum - Text Fragment Generation Content Script
// // Composition root - orchestrates all modules
//
// import { createToast } from "@/ui/toast";
// import { attachEventListeners } from "@/events";
//
// /**
//  * Initialize the Fragmentum extension
//  * Sets up UI components and attaches event listeners
//  */
// function init(): void {
//   // Create UI components
//   createToast();
//
//   // Attach the event listeners
//   attachEventListeners();
// }
//
// // Initialize when DOM is ready
// if (document.readyState === "loading") {
//   document.addEventListener("DOMContentLoaded", init);
// } else {
//   init();
// }

// Content script is disabled - no initialization needed
export {};
